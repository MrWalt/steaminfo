import SearchBar from "./SearchBar";

export default function Header() {
  return (
    <div className="w-full bg-primary-700 py-6 px-[64px] border-b border-primary-500">
      <SearchBar />
    </div>
  );
}
